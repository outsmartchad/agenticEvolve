# agenticEvolve v2 gateway dependencies

# Core
pyyaml>=6.0

# Telegram (optional — gateway skips if not installed)
python-telegram-bot>=21.0

# Discord (optional — gateway skips if not installed)
discord.py>=2.3

# WhatsApp uses Node.js bridge (see whatsapp-bridge/package.json)
